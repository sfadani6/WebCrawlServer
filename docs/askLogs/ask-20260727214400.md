# 작업 요청 및 처리 이력

- **일시**: 2026-07-27 21:44:00
- **요청자**: 사용자
- **작업 내용**: 웹소켓(WebSocket) 개발 가이드라인 신규 규칙 문서(`docs/rule/websocket.md`, R-016) 작성 및 Rule Registry(`AGENTS.md`, `docs/rule/instructions.md`, `mcp.md`) 연동 개선

---

## 1. 주요 작성 및 변경 내역
- **웹소켓 규격 문서 신설 (`docs/rule/websocket.md` / R-016)**:
  - 서버-클라이언트(브라우저 플러그인, Admin UI, MCP 모듈) 간 WebSocket 연결 수명주기 및 `WS_TOKEN` 인증 흐름 정의
  - Chrome MV3 Service Worker 휴면(Idle Shutdown) 대응 `chrome.alarms` 킵얼라이브 지침
  - 지수 백오프(Exponential Backoff) 재연결 및 서킷 브레이커, 오프라인 메시지 큐ing (`chrome.storage.local`) 규격화
  - 대용량 스크래핑 데이터 패킷 분할(Chunking) 전송 및 AI 개발자 준수 체크리스트 명시
- **Rule Registry 및 인덱스 문서 갱신**:
  - `AGENTS.md`: 0.2 Rule Registry 표 및 문서 계층 설명에 `R-016 (websocket.md)` 등록
  - `docs/rule/instructions.md`: R-000 인덱스 표에 `R-016` 추가 및 범위 업데이트
  - `docs/rule/mcp.md`: 6장(전송 채널) 및 관련 문서 목록에 `websocket.md (R-016)` 상호 참조 연동

---

## 2. 변경 파일
- `docs/rule/websocket.md`: 웹소켓 통신 가이드라인 신규 작성 (R-016)
- `AGENTS.md`: Rule Registry 표 R-016 반영 및 버전 정보 업데이트
- `docs/rule/instructions.md`: R-000 인덱스 R-016 반영
- `docs/rule/mcp.md`: WebSocket 세부 가이드 R-016 상호 참조 연동
- `docs/askLogs/ask-20260727214400.md`: 본 작업 이력 작성
- `docs/todo.history.md`: 작업 처리 내역 기록
