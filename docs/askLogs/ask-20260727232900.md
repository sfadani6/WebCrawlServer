# 작업 요청 및 처리 이력

- **일시**: 2026-07-27 23:29:00
- **요청자**: 사용자
- **작업 내용**: 소스 코드 전체 구조 분석 기반 리팩토링 및 고도화 과제 도출 후 `docs/todo.md` 반영 및 GitHub 푸시

---

## 1. 소스 코드 분석 및 도출된 6대 리팩토링/고도화 영역

### 1.1 백엔드 코어 & 서버 아키텍처 개선 (Backend Core Architecture)
- `server/app.js` 모듈화 및 Graceful Shutdown 수명주기 관리 분리.
- `routes/` -> `services/` 관심사 분리(SoC)를 위한 서비스 계층 도입.
- 중앙 집중식 커스텀 에러 핸들러(`asyncHandler`, `AppError`) 및 스키마 검증 미들웨어 도입.

### 1.2 워크플로우 & 스케줄러 실행 엔진 고도화 (Engine Optimization)
- `workflowEngine.js` 실행 컨텍스트 격리, 무한 루프 서킷 브레이커 및 `AbortController` 기반 취소/지수 백오프 재시도 메커니즘 구축.
- `jobRunner.js` 동시성 작업 큐(Concurrency Limiter) 및 중복 실행 방지 락(Locking) 구축.

### 1.3 SQLite DB & 저장소 관리 최적화 (Database & Persistence)
- `server/db/helper.js` WAL(Write-Ahead Logging) 모드 및 `busy_timeout` 설정으로 동시성 충돌 방지.
- DB 트랜잭션 래퍼 함수(`db.transaction()`) 및 동적 스키마 검증 강화.

### 1.4 브라우저 플러그인 리팩토링 (Extension Manifest V3 Refactoring)
- `plugin/background.js` Service Worker 재시동 시 WebSocket 연결 및 오프라인 메시지 큐 복구 로직.
- `contentScript.js` 대용량 DOM 스크래핑 비동기 Chunk 수집.

### 1.5 관리자 UI 프론트엔드 코드 구조 개선 (Frontend Refactoring)
- `server/admin-ui/src/api.js` 에러 인터셉터, 자동 재시도 및 Toast 연동.
- 대형 컴포넌트(`VisualWorkflowEditor`, `SpreadsheetView`, `ConnectionPage`) 커스텀 훅 추출.
- 전역 상태 관리(Zustand / Context) 도입을 통한 Props Drilling 해소.

### 1.6 보안, 모니터링 & 테스트 커버리지 강화 (Security & Testing)
- Rate Limiting 및 Security Headers 미들웨어 적용.
- 핵심 모듈 단위/통합 테스트 코드 확충 (Test Coverage > 80%).

---

## 2. 변경 파일
- `docs/todo.md`: 프로그램 소스 분석 기반 리팩토링 및 고도화 6대 과제 추가
- `docs/askLogs/ask-20260727232900.md`: 본 작업 이력 문서 생성
- `docs/todo.history.md`: 작업 처리 내역 기록
