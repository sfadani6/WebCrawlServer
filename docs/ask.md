## 백엔드 미구현 기능 (P1)

| 우선순위 | 작업 항목 | 상태 | 상세 |
|----------|----------|------|------|
| P1 | **admin-ui/dist 빌드 산출물 확인** | 🟢 확인 완료 | `server/admin-ui/dist/index.html` 존재 확인. 빌드 산출물 확보 |


## 프론트엔드 개선 (P2)

| 우선순위 | 작업 항목 | 상태 | 상세 |
|----------|----------|------|------|
| P2 | **테이블 데이터 페이지네이션, 정렬, 검색 필터 UI 추가** | 🟡 미구현 | `SpreadsheetView.jsx`에 상태 변수 추가 완료. 실제 UI/로직 추가 필요 |
| P2 | **테이블 CSV/JSON 내보내기/가져오기 기능** | 🟡 구현 중 | CSV/JSON 내보내기 구현 완료. 가져오기는 백엔드 미구현으로 UI 제한 |
| P2 | **CSS 중복 규칙 정리 및 디자인 토큰 문서 갱신** | 🟢 정리 완료 | `App.css`는 거의 비어 있고 `index.css`에 디자인 토큰이 집중되어 있어 중복이 없음 |
| P2 | **SPA 라우팅 뒤로가기 시 상태 복원** | 🟢 구현 완료 | `sessionStorage`에 선택된 DB/테이블을 저장하고 `popstate` 시 복원하도록 구현 |

---
---

## 아키텍처/설계 개선 (P2)

| 우선순위 | 작업 항목 | 상태 | 상세 |
|----------|----------|------|------|
| P2 | **DB 연결 풀(Connection Pool) 도입** | 🔴 개선 필요 | 모든 모듈에서 매 요청/함수 호출마다 새 DB 연결 생성 후 즉시 종료. `better-sqlite3` 또는 연결 풀 패턴 도입 필요 |
| P2 | **monitorWs.js 리소스 로그 과다 기록** | 🟢 개선 완료 | 이미 10초마다 전송하되 DB 기록은 5분 간격 샘플링으로 제한됨 |
| P2 | **에러 처리 미들웨어 통합** | 🟢 통합 완료 | `server/app.js`에 표준 에러 핸들러가 적용되어 있음 |
| P2 | **API 응답 형식 표준화** | 🟡 부분 개선 | 에러 응답은 표준화되었으나, 성공 응답 형식이 `/api`, `/admin/api`마다 다름. 추가 통합 필요 |

---


## 보안 강화 (P2)

| 우선순위 | 작업 항목 | 상태 | 상세 |
|----------|----------|------|------|
| P2 | **레이트 리밋 바이패스 가능성** | 🟢 확인 완료 | `/api`, `/admin/api`, `/api/nlp`에 각각 `apiLimiter`, `adminApiLimiter`, `nlpLimiter` 적용되어 있음 |
| P2 | **민감 정보 로그 노출** | 🟢 개선 완료 | 기존 로그는 구조적 필드만 출력. 추가 마스킹은 선택적 개선 |

---

## 테스트 및 문서화 (P3)

| 우선순위 | 작업 항목 | 상태 | 상세 |
|----------|----------|------|------|
| P3 | **단위 테스트 도입** | 🟢 완료 | Jest 설치, `npm test` 스크립트 추가, server/scripts/scriptEngine.test.js 작성 완료 |
| P3 | **통합 테스트 도입** | 🟢 완료 | supertest 설치 및 server/app.test.js 작성 완료 (health, api, nlp, 404 테스트) |
| P3 | **docs/rule/ 문서와 실제 구현 간 불일치 확인** | 🟢 확인 완료 | R-005(scheduler), R-006(monitoring), R-007(database), R-009(logging) 등 주요 문서는 코드와 일치 확인 |
| P3 | **API 문서화 (Swagger/OpenAPI)** | 🟢 완료 | `docs/api-spec.md` 작성 완료. 모든 API 엔드포인트, 응답 형식, 인증 방식 문서화 |

---

## 인프라/운영 (P3)

| 우선순위 | 작업 항목 | 상태 | 상세 |
|----------|----------|------|------|
| P3 | **데이터베이스 마이그레이션 스크립트** | 🟢 완료 | `server/scripts/migrate.js` 작성 완료. `server/migrations/` 디렉토리의 SQL 파일을 순서대로 실행 |

---