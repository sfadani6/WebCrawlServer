관리자 페이지에 플러그인과 연결 정보를 관리하는곳이 없어 
ask 처리해 라던지 todo.md 내용을 처리할때면
각 항목별 처리할 내역 과 처리한 결과를 
docs/askLogs 폴더에 ask-YYYYMMDDHHMMSS.md 형식으로 기록하고
ask-YYYYMMDDHHMMSS.md 에는 
지금의 ask.md에 처리 결롸를 기록및 Task Completed 내용을 기록해 
docs/todo.history.md 에는 처리한 이력을 기록하고
docs/todo.md 에는 처리한 항목의 상태를 갱신하고
- 완료된 항목은 삭제 
docs/CHANGELOG.md 에는 처리한 항목의 변경사항을 기록하고
자세한 내역은 
docs/CHANGELOG/ 폴더에 처리항목-YYYYMMDDHHMMSS.md 형식으로 기록하고
그리고 ask.md는 비워줘
무조껀 AI가 작업한 진행 흐름은 강제 규정이야 
가이드 지침의 내역을 수정 개선해줘 그리고 즉비 반영해


git config --global user.email "sfadani5@gmail.com"
git config --global user.name "sfadani5"

pluginManager.js에 각 플러그인의 탭/스크립트 상세 정보를 조회할 수 있는 메서드를 추가하고, 관련 API를 /admin/api/plugins/:id/resources로 분리해줘.
websocketService.js에서 MCP 프로토콜의 'script' 타입 메시지를 처리하여 플러그인에 스크립트를 전달하는 handleScript 메서드를 구현해줘.

plugin/background.js에서 서버의 'script' 타입 메시지를 수신하여 실제 contentScript로 전달하는 로직을 구현해줘.
/admin/api/plugins/:id/resources API 결과를 대시보드에서 클릭 시 팝업 창으로 상세히 보여주는 UI 기능을 추가해줘.

서버의 서비스 기능들을 분석해서 개선점을 todo.md 에 적어줘
관리자 페이지및 각 웹서비스의 모든 기능들을 분석해서 개선점 오류등을 찾아서 todo.md 에 적어줘
브라우져 플러그인의 소스를분석하고 서버및 서비스들에 맞게 되었는지 분석해서 todo.md 에 적어줘
현재프로젝트의 모든 부분의 개발 항목별로 개선점 검토사항등을 todo.md 에 적어줘


pluginManager.js에서 DB 연결을 싱글톤으로 관리하도록 리팩토링하고, 모듈별 DB 파일을 자동으로 생성하는 로직을 추가해줘.
websocketService.js의 handleMessage 함수에 JSON Schema를 이용한 MCP 프로토콜 검증 미들웨어를 적용해줘.
- [ ] **서버 시작 로직 간소화**: DB 로딩 및 캐시 초기화 과정을 서비스 레이어로 추가 캡슐화 검토
- [ ] **데이터베이스 커넥션 관리 개선**: `pluginManager.js` 및 `websocketService.js`에서 매번 `new sqlite3.Database`를 호출하는 대신, 공유 커넥션 풀 또는 싱글톤 인스턴스 사용 (성능 최적화)
- [ ] **MCP 프로토콜 검증 강화**: `websocketService.js` 내 메시지 스키마 검증 로직을 JSON Schema 기반으로 고도화하여 잘못된 형식의 메시지 차단

- [ ] **통합 관리자 셸(Shell) 구축**: 모든 관리자 페이지에 공통 상단바와 사이드바를 적용하여 구글 클라우드 콘솔 스타일의 일관된 UX 제공
- [ ] **모듈별 독립 DB 자동 생성**: 신규 모듈 등록 시 `module_{id}.db` 파일을 자동으로 생성하고 관련 테이블 스키마(`collected_data` 등)를 초기화하는 기능 추가
- [ ] **데이터 시각화 고도화**: 대시보드 차트의 시간 범위를 사용자가 선택할 수 있는 기능 추가

### 브라우저 플러그인 연동 최적화
- [ ] **V3 서비스 워커 생명주기 대응**: 플러그인 서비스 워커 종료 시 WebSocket 연결을 유지하거나 즉시 재연결하는 백그라운드 동기화 전략 수립
- [ ] **스크립트 실행 상태 보고 채널**: 서버의 `handleScript` 명령에 대해 플러그인이 `running`, `completed`, `failed` 등 상세 상태를 `result` 타입을 통해 보고하도록 프로토콜 보완
- [ ] **플러그인 UI 액션 매핑**: `send_page`, `send_screenshot` 외에 사용자가 정의한 커스텀 액션 버튼을 플러그인 팝업에 동적으로 렌더링하는 기능
- [ ] **활동 로그 보관 정책(Retention)**: 오래된 로그를 자동으로 아카이빙하거나 삭제하는 배치 스크립트 작성
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능




server/utils/schemaValidator.js에 push_module 및 result 메시지 타입에 대한 상세 페이로드 스키마를 지침 5.3에 맞춰 추가해줘.
systemService.js에 서버 종료 시(SIGTERM) DB 커넥션을 안전하게 닫는(Graceful Shutdown) 로직을 구현해줘.
서버의 프론트부분의 모든 기능을 검토하고 개선점 오류등을 todo.md에 적어줘

server/admin-ui 프로젝트의 Tailwind 설정을 수정하여 전체적인 폰트 크기를 줄이고 테이블 레이아웃을 콤팩트하게 변경해줘.
plugin/background.js에서 서버의 SIGTERM 감지 시 플러그인에 연결 종료 알림을 보내고 재연결 백오프를 시작하는 로직을 추가해줘.
- [ ] **활동 로그 보관 정책(Retention)**: 오래된 로그를 자동으로 아카이빙하거나 삭제하는 배치 스크립트 작성
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능
- [ ] **V3 서비스 워커 생명주기 대응**: 플러그인 서비스 워커 종료 시 WebSocket 연결을 유지하거나 즉시 재연결하는 백그라운드 동기화 전략 수립
- [ ] **스크립트 실행 상태 보고 채널**: 서버의 `handleScript` 명령에 대해 플러그인이 `running`, `completed`, `failed` 등 상세 상태를 `result` 타입을 통해 보고하도록 프로토콜 보완
- [ ] **플러그인 UI 액션 매핑**: `send_page`, `send_screenshot` 외에 사용자가 정의한 커스텀 액션 버튼을 플러그인 팝업에 동적으로 렌더링하는 기능
- [ ] **데이터 시각화 고도화**: 대시보드 차트의 시간 범위를 사용자가 선택할 수 있는 기능 추가
- [ ] **데이터 밀도 최적화**: 대시보드 및 로그 테이블의 폰트 크기를 조정하고 불필요한 여백을 줄여 정보 가독성 향상
- [ ] **레거시 UI 교체**: `public/index.html` 기반의 초기 카드 디자인을 React 기반의 통합 대시보드로 완전히 이관
- [ ] **실시간 상태 동기화**: 플러그인 목록 및 리소스 상태를 폴링 방식에서 WebSocket Push 방식으로 전환하여 UI 반응성 개선
- [ ] **오류 시각화 개선**: MCP 프로토콜 검증 실패(`INVALID_SCHEMA`) 시 상세 에러 내용을 관리자 콘솔에서 JSON 형태로 즉시 확인할 수 있는 뷰어 추가




plugin/background.js 파일의 전체 코드를 작성하여 서비스 워커 생명주기와 재연결 백오프 로직을 통합해줘.
server/services/logRetentionService.js를 생성하여 activity_logs 테이블의 오래된 데이터를 자동 아카이빙하는 기능을 구현해줘.
- [ ] **통합 관리자 셸(Shell) 구축**: 모든 관리자 페이지에 공통 상단바와 사이드바를 적용하여 구글 클라우드 콘솔 스타일의 일관된 UX 제공
- [ ] **데이터 시각화 고도화**: 대시보드 차트의 시간 범위를 사용자가 선택할 수 있는 기능 추가
- [ ] **데이터 밀도 최적화**: 대시보드 및 로그 테이블의 폰트 크기를 조정하고 불필요한 여백을 줄여 정보 가독성 향상
- [ ] **레거시 UI 교체**: `public/index.html` 기반의 초기 카드 디자인을 React 기반의 통합 대시보드로 완전히 이관
- [ ] **실시간 상태 동기화**: 플러그인 목록 및 리소스 상태를 폴링 방식에서 WebSocket Push 방식으로 전환하여 UI 반응성 개선
- [ ] **오류 시각화 개선**: MCP 프로토콜 검증 실패(`INVALID_SCHEMA`) 시 상세 에러 내용을 관리자 콘솔에서 JSON 형태로 즉시 확인할 수 있는 뷰어 추가
- [ ] **V3 서비스 워커 생명주기 대응**: 플러그인 서비스 워커 종료 시 WebSocket 연결을 유지하거나 즉시 재연결하는 백그라운드 동기화 전략 수립
- [ ] **스크립트 실행 상태 보고 채널**: 서버의 `handleScript` 명령에 대해 플러그인이 `running`, `completed`, `failed` 등 상세 상태를 `result` 타입을 통해 보고하도록 프로토콜 보완
- [ ] **플러그인 UI 액션 매핑**: `send_page`, `send_screenshot` 외에 사용자가 정의한 커스텀 액션 버튼을 플러그인 팝업에 동적으로 렌더링하는 기능
- [ ] **활동 로그 보관 정책(Retention)**: 오래된 로그를 자동으로 아카이빙하거나 삭제하는 배치 스크립트 작성
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능
- [ ] **활동 로그 보관 정책(Retention)**: 오래된 로그를 자동으로 아카이빙하거나 삭제하는 배치 스크립트 작성
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능
- [ ] **에러 복구 자동화**: 플러그인 연결이 끊겼을 때 서버에서 해당 플러그인이 수행하던 태스크를 대기열로 돌리거나 다른 플러그인으로 재배정하는 로직 (P1 작업의 확장)

plugin/background.js에서 스크립트 엔진(executeSteps)의 전체 구현 코드를 작성하여 실제 브라우저 자동화 동작을 완성해줘.
server/admin-ui에서 activity_logs_archive 테이블의 데이터를 날짜별로 조회하거나 CSV로 내보내는 기능을 추가해줘.
todo.md에서 미해결된  부분을들 전부 개선및 처리해줘


- [ ] **레거시 UI 교체**: `public/index.html` 기반의 초기 카드 디자인을 React 기반의 통합 대시보드로 완전히 이관
server/admin-ui (React) 프로젝트에서 방금 만든 /logs/archive API를 사용하는 전용 아카이브 뷰어 컴포넌트를 작성해줘.
plugin/contentScript.js에서 실제 'waitFor'와 'extract' 명령을 처리하는 DOM 조작 로직을 보강해줘.


레거시 UI의전체를 검토해서 서버 정적 경로를 React 빌드 폴더로 변경 및 통합 하는 방식의 개선안을 모든 레거시 UI의전체를 검색해서 검토하고 todo.md 에 적어줘
ArchiveLogViewer 컴포넌트를 server/admin-ui의 실제 라우팅(App.jsx)에 등록하고 사이드바 메뉴에 추가해줘.
plugin/contentScript.js에 'send_screenshot' 액션 발생 시 Canvas를 이용해 페이지의 특정 영역만 크롭하여 전송하는 로직을 추가해줘.


plugin/background.js에 wcs_internal_captureTab 메시지 핸들러를 추가하여 실제 탭 캡처 데이터를 콘텐츠 스크립트에 전달하는 로직을 구현해줘.
server/admin-ui/src/App.jsx 파일 전체를 생성하여 ArchiveLogViewer와 사이드바 레이아웃이 적용된 현대적인 관리자 UI를 완성해줘.
- [ ] **활동 로그 보관 정책(Retention)**: 오래된 로그를 자동으로 아카이빙하거나 삭제하는 배치 스크립트 작성
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능
ping 을 보내는 주기는 환겨얼정부분에서 설정하게 해줘 
- [ ] **레거시 기능 완전 이관 (Full Migration)**: 
    - `public/index.html`에 남아있는 실시간 Chart.js 로직을 React Recharts/Chart.js 컴포넌트로 재구현
    - 설정 모달(관리자 계정 변경, 폴링 주기 설정)을 React 전용 Settings 페이지로 이관
    - `/admin/api/plugins` 호출부의 상태 관리 로직을 React Context 또는 전역 Store로 통합
    - 이관 완료 후 `public/` 디렉토리 하위 레거시 파일군(`index.html`, `js/`, `css/`) 완전 삭제 및 `app.js`에서 관련 미들웨어 제거
- [ ] **실시간 상태 동기화**: 플러그인 목록 및 리소스 상태를 폴링 방식에서 WebSocket Push 방식으로 전환하여 UI 반응성 개선
- [ ] **오류 시각화 개선**: MCP 프로토콜 검증 실패(`INVALID_SCHEMA`) 시 상세 에러 내용을 관리자 콘솔에서 JSON 형태로 즉시 확인할 수 있는 뷰어 추가

서버와 프론트 페이지들을 검토하고 기능및 UI 개선안을 todo.md에 만들어줘
서버와 프론트 페이지들의 모든 소스를 검토해서 효율적인 리팩토리 개선안을 검토해서 todo.md에 적어줘
서버와 프론트 페이지들을 검토하고 기능및 UI 개선안및 아이디어를 todo.md에 적어줘
server/db/init.js 파일에 'Active Ping 주기(ms)' 설정을 위한 신규 시딩 SQL 구문을 추가해줘.
server/admin-ui에서 Recharts를 사용하여 CPU/메모리 사용량을 실시간으로 보여주는 전용 차트 컴포넌트를 만들어줘.

server/admin-ui/src/components/OverviewPage.jsx를 수정하여 방금 만든 ResourceChart 컴포넌트를 상단 대시보드에 배치해줘.
server/db/helper.js를 리팩토링하여 모든 DB 쿼리 실행을 Promise 기반의 Repository 클래스로 전환하는 구조를 제안해줘.
- [ ] **Repository 패턴 도입**: `pluginManager.js` 및 라우터에 흩어진 `sqlite3` 직접 호출 로직을 전용 Repository/DAO 클래스로 캡슐화
- [ ] **중앙 집중형 에러 핸들러**: `websocketService.js`와 API 라우터의 `try-catch` 중복을 줄이기 위해 전역 비동기 에러 래퍼 적용
- [ ] **플러그인 커맨드 핸들러 맵**: `background.js`의 거대한 `switch-case` 문을 핸들러 맵 객체 구조로 분리하여 확장성 개선
- [ ] **로깅 라이브러리 교체**: `console.log` 대신 `Winston` 또는 `Pino`를 사용하여 파일/DB 로그 레벨 관리 표준화
- [ ] **시각적 워크플로우 에디터**: YAML 직접 편집 대신 `React Flow`를 이용한 노드 기반 드래그 앤 드롭 워크플로우 설계 기능
- [ ] **글로벌 다크모드 지원**: Tailwind 테마 설정을 통한 운영자용 다크모드 UI 옵션 추가
- [ ] **플러그인 원격 터미널**: 관리자 페이지에서 특정 플러그인의 브라우저 콘솔 로그를 실시간으로 확인하고 JS를 주입하는 디버깅 도구
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능
- [ ] **토큰 회전(Rotation) 구현**: `AuthService` 및 `websocketService`에 Refresh Token 재사용 감지 및 자동 회전 로직 실제 구현 (보안 강화)
- [ ] **Windows 실행 파일 보안 검증**: `windows_exec` 런타임 사용 시 SHA-256 해시 allowlist를 검사하는 미들웨어 로직 실제 구현
- [ ] **활동 로그 보관 정책(Retention)**: 오래된 로그를 자동으로 아카이빙하거나 삭제하는 배치 스크립트 작성
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능



server/routes/admin.js의 로그 조회 및 아카이브 조회 로직을 신규 Repository 클래스를 사용하도록 리팩토링해줘.
server/admin-ui에서 OverviewPage.jsx를 App.jsx의 메인 라우트로 연결하고 정상적으로 렌더링되도록 수정해줘.
todo.md에거 미구현된 항목들을 처리해줘


server/app.js에 전역 에러 핸들러 미들웨어를 추가하여 asyncHandler에서 던진 에러를 표준 JSON 형식으로 응답하도록 구현해줘.
server/admin-ui에 Tailwind 다크모드 기능을 활성화하고, App.jsx에 테마 전환 토글 버튼을 추가해줘.
- [ ] **시각적 워크플로우 에디터**: YAML 직접 편집 대신 `React Flow`를 이용한 노드 기반 드래그 앤 드롭 워크플로우 설계 기능
- [ ] **글로벌 다크모드 지원**: Tailwind 테마 설정을 통한 운영자용 다크모드 UI 옵션 추가
- [ ] **플러그인 원격 터미널**: 관리자 페이지에서 특정 플러그인의 브라우저 콘솔 로그를 실시간으로 확인하고 JS를 주입하는 디버깅 도구

server/admin-ui (React) 프로젝트의 주요 컴포넌트들(OverviewPage, LogsPage 등)에 dark: 접두사를 사용한 다크모드 전용 색상을 적용해줘.
websocketService.js에도 asyncHandler와 유사한 형태의 에러 래퍼를 적용하여 WebSocket 메시지 처리 중 발생하는 에러를 표준 MCP 에러 메시지로 자동 변환하게 해줘.



server/admin-ui의 SettingsPage.jsx 컴포넌트에도 다크모드 전용 스타일을 적용하고, 폼 입력 필드의 가독성을 개선해줘.
plugin/background.js에서 서버의 error_code(SERVER_ERROR 등)를 감지했을 때 사용자에게 알림을 띄우는 로직을 추가해줘.
관리자의 환경 변수셋팅 안에 들어갈수있는 항목들을 분류대로 모두 찾아서 개선사항으로 todo.md 에 할일 목록으로 기록해줘 

server/admin-ui/src/components/SettingsPage.jsx에서 폼 제출 시 성공/실패 여부를 사용자에게 알려주는 Toast 알림 컴포넌트를 추가해줘.
plugin/manifest.json에 'notifications' 권한을 추가하고, 에러 상황별로 아이콘을 다르게 표시하도록 background.js 로직을 보강해줘.
관리자의 환경 변수셋팅 안에 들어갈수있는 항목들을 분류대로 모두 찾아서 개선사항으로 todo.md 에 할일 목록으로 기록해줘 
- [ ] **시각적 워크플로우 에디터**: YAML 직접 편집 대신 `React Flow`를 이용한 노드 기반 드래그 앤 드롭 워크플로우 설계 기능
- [ ] **글로벌 다크모드 지원**: Tailwind 테마 설정을 통한 운영자용 다크모드 UI 옵션 추가
- [ ] **플러그인 원격 터미널**: 관리자 페이지에서 특정 플러그인의 브라우저 콘솔 로그를 실시간으로 확인하고 JS를 주입하는 디버깅 도구





플러그인을 오페라 브라우져에 설치했는데
확장프로그램이 계속해서 
네트워크오류 websocket 연결 오류로 메제시가 나오고
관리자 화면에서도 접속신청이나 시도같은 화면을 볼수있는게 아무것도없어

확장프로그램 로드 못함

파일
E:\up\0-project\git\xtx9999\WebCrawlServer\plugin
오류
Could not load icon 'icons/icon16.png' specified in 'action'.
매니페스트를 로드할 수 없습니다.

---

플러그인은 메뉴에 서버 접속 이라는 버튼을 만들고 클릭하면 서버에 플러그인이 탑재된 브라우져 정도와함께 서버 플러그인 메뉴에 접속 요청이라는게 나타나고 서버관리자 화면 플러그인 메뉴에서 접속요청한 플러그인을 로그인승인하면 서버에ㅓ 플러그인에 연결 토큰을 전송 해줘 플러그인은 연결 토큰을 가지고 서버와 지속적인 연결이 가능해야되 




플러그인 로그에서 
WebSocket connection to 'ws://localhost:9600/' failed: HTTP Authentication failed; no valid credentials available
컨텍스트
background.js
스택 추적
background.js:88 (익명 함수)
[WCS] WebSocket 오류: [object Event]
컨텍스트
background.js
스택 추적
background.js:115 (익명 함수)

계속 오페라 알람으로떠 

관리자 페이지 플러그인 페이지에서 접속및 연결 요청 플러그인 목록이나와야될거  같은데 

ask 처리해 라던지 todo.md 내용을 처리할때면
각 항목별 처리할 내역 과 처리한 결과를 
docs/askLogs 폴더에 ask-YYYYMMDDHHMMSS.md 형식으로 기록하고
ask-YYYYMMDDHHMMSS.md 에는 
지금의 ask.md에 처리 결롸를 기록및 Task Completed 내용을 기록해 
docs/todo.history.md 에는 처리한 이력을 기록하고
docs/todo.md 에는 처리한 항목의 상태를 갱신하고
- 완료된 항목은 삭제 
docs/CHANGELOG.md 에는 처리한 항목의 변경사항을 기록하고
자세한 내역은 
docs/CHANGELOG/ 폴더에 처리항목-YYYYMMDDHHMMSS.md 형식으로 기록하고
그리고 ask.md는 비워줘
무조껀 AI가 작업한 진행 흐름은 강제 규정이야 
가이드 지침의 내역을 수정 개선해줘 그리고 즉비 반영해


git config --global user.email "sfadani5@gmail.com"
git config --global user.name "sfadani5"

pluginManager.js에 각 플러그인의 탭/스크립트 상세 정보를 조회할 수 있는 메서드를 추가하고, 관련 API를 /admin/api/plugins/:id/resources로 분리해줘.
websocketService.js에서 MCP 프로토콜의 'script' 타입 메시지를 처리하여 플러그인에 스크립트를 전달하는 handleScript 메서드를 구현해줘.

plugin/background.js에서 서버의 'script' 타입 메시지를 수신하여 실제 contentScript로 전달하는 로직을 구현해줘.
/admin/api/plugins/:id/resources API 결과를 대시보드에서 클릭 시 팝업 창으로 상세히 보여주는 UI 기능을 추가해줘.

서버의 서비스 기능들을 분석해서 개선점을 todo.md 에 적어줘
관리자 페이지및 각 웹서비스의 모든 기능들을 분석해서 개선점 오류등을 찾아서 todo.md 에 적어줘
브라우져 플러그인의 소스를분석하고 서버및 서비스들에 맞게 되었는지 분석해서 todo.md 에 적어줘
현재프로젝트의 모든 부분의 개발 항목별로 개선점 검토사항등을 todo.md 에 적어줘


pluginManager.js에서 DB 연결을 싱글톤으로 관리하도록 리팩토링하고, 모듈별 DB 파일을 자동으로 생성하는 로직을 추가해줘.
websocketService.js의 handleMessage 함수에 JSON Schema를 이용한 MCP 프로토콜 검증 미들웨어를 적용해줘.
- [ ] **서버 시작 로직 간소화**: DB 로딩 및 캐시 초기화 과정을 서비스 레이어로 추가 캡슐화 검토
- [ ] **데이터베이스 커넥션 관리 개선**: `pluginManager.js` 및 `websocketService.js`에서 매번 `new sqlite3.Database`를 호출하는 대신, 공유 커넥션 풀 또는 싱글톤 인스턴스 사용 (성능 최적화)
- [ ] **MCP 프로토콜 검증 강화**: `websocketService.js` 내 메시지 스키마 검증 로직을 JSON Schema 기반으로 고도화하여 잘못된 형식의 메시지 차단

- [ ] **통합 관리자 셸(Shell) 구축**: 모든 관리자 페이지에 공통 상단바와 사이드바를 적용하여 구글 클라우드 콘솔 스타일의 일관된 UX 제공
- [ ] **모듈별 독립 DB 자동 생성**: 신규 모듈 등록 시 `module_{id}.db` 파일을 자동으로 생성하고 관련 테이블 스키마(`collected_data` 등)를 초기화하는 기능 추가
- [ ] **데이터 시각화 고도화**: 대시보드 차트의 시간 범위를 사용자가 선택할 수 있는 기능 추가

### 브라우저 플러그인 연동 최적화
- [ ] **V3 서비스 워커 생명주기 대응**: 플러그인 서비스 워커 종료 시 WebSocket 연결을 유지하거나 즉시 재연결하는 백그라운드 동기화 전략 수립
- [ ] **스크립트 실행 상태 보고 채널**: 서버의 `handleScript` 명령에 대해 플러그인이 `running`, `completed`, `failed` 등 상세 상태를 `result` 타입을 통해 보고하도록 프로토콜 보완
- [ ] **플러그인 UI 액션 매핑**: `send_page`, `send_screenshot` 외에 사용자가 정의한 커스텀 액션 버튼을 플러그인 팝업에 동적으로 렌더링하는 기능
- [ ] **활동 로그 보관 정책(Retention)**: 오래된 로그를 자동으로 아카이빙하거나 삭제하는 배치 스크립트 작성
- [ ] **플러그인 헬스체크 고도화**: 서버에서 주기적으로 플러그인에 `ping`을 보내 응답 속도 및 리소스 상태를 수집하는 기능




server/utils/schemaValidator.js에 push_module 및 result 메시지 타입에 대한 상세 페이로드 스키마를 지침 5.3에 맞춰 추가해줘.
systemService.js에 서버 종료 시(SIGTERM) DB 커넥션을 안전하게 닫는(Graceful Shutdown) 로직을 구현해줘.


관리자 페이지에 
메뉴 : 접속관리
여기서는 현재 http://localhost:9600 접속해있는 장비 리스트들이 나와야되고  접속 브라우져 버전 로그인 되어있는지 확인이 가능해야하고 
메뉴 : 플러그인
여기서는 접속 플러그인들 - 브라우져 정보 , 프로세서정보 , 연결 정보들이 들어가야되 
우선 이걸 만들어줘 












